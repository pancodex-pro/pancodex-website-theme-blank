import {DataFieldConfig} from '@pancodex/sdk-lib';

export const twitterImage: DataFieldConfig = {
    label: 'Twitter card image (small)',
    indexNumber: 0,
    dataType: 'string',
    inputType: 'image',
    defaultValue: ''
}
