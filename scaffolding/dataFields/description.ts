import {DataFieldConfig} from '@pancodex/sdk-lib';

export const description: DataFieldConfig = {
    label: 'Page Description',
    indexNumber: 0,
    dataType: 'string',
    inputType: 'text',
    defaultValue: 'Some page description'
}